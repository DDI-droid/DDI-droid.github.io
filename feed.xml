<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://ddi-droid.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://ddi-droid.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-24T18:44:02+00:00</updated><id>https://ddi-droid.github.io/feed.xml</id><title type="html">blank</title><subtitle>Anshul Thakur — researcher working on Policy Language Models, meta-reasoning, and LLM inference-time scaling. </subtitle><entry><title type="html">Policy Language Models</title><link href="https://ddi-droid.github.io/blog/2026/policy-language-models/" rel="alternate" type="text/html" title="Policy Language Models"/><published>2026-06-30T12:00:00+00:00</published><updated>2026-06-30T12:00:00+00:00</updated><id>https://ddi-droid.github.io/blog/2026/policy-language-models</id><content type="html" xml:base="https://ddi-droid.github.io/blog/2026/policy-language-models/"><![CDATA[<p>This post introduces what I’m working on right now, Policy Language Models. The paper is linked at the end.</p> <h2 id="the-trajectory">The trajectory</h2> <p>Frontier large language models and the harnesses built around them can now tackle complex long-horizon problems, resolve software issues, conduct deep research, and drive hours-long sessions. The gains that enabled this have largely arrived through inference-time scaling. Models are trained to spend more tokens deliberating, and ReAct systems extend those capabilities by interleaving reasoning with actions and observations. ReAct remains the dominant substrate for this work, and a ReAct trajectory is a growing sequence of those thoughts, actions, and observations.</p> <p>The trajectory only grows, every thought, action, and observation is appended, and LLM outputs degrade as the context window fills. The most popular general remedy is context compaction, where the trajectory is repeatedly summarized when it exceeds a length threshold. Compaction loses information, and it does not repair the reasoning inside the window. Trajectories pushed to length fail in characteristic ways, exploration that fragments the reasoning, self-reflection that strains against load-bearing mistakes, the capacity of the window itself; the short version is a single mismatch. The model repeatedly reaches for reasoning shapes the autoregressive substrate cannot hold: it branches to weigh alternatives, reverts to undo a wrong step, and explores before committing, yet every such move is flattened back into one forward sequence. We believe these failures act as a dampener on reasoning LLMs, and that solving them will yield far more capable reasoners.</p> <h2 id="taking-work-off-the-line">Taking work off the line</h2> <p>Recently, <a href="https://arxiv.org/abs/2512.24601">Zhang, Kraska, and Khattab</a> introduced Recursive Language Models (RLMs), a general-purpose inference paradigm for dramatically scaling the effective input and output lengths of LLMs. Their key insight is that an arbitrarily long prompt should not be fed into the neural network directly, but treated as part of the environment that the LLM symbolically and recursively interacts with. Given a prompt \(P\), an RLM initializes a REPL environment in which \(P\) is set as the value of a variable, and the model writes code that peeks into and decomposes \(P\), observes the side effects of execution, and invokes the LLM itself on as many slices of the input as necessary. What leaves the trajectory, though, is the input/output processing alone. Thinking, exploration, reflection, action, and observation still accumulate on it, and the thinking is still done the ReAct way.</p> <h2 id="policy-language-models">Policy Language Models</h2> <p>We introduce Policy Language Models (PLMs), a general-purpose meta-reasoning inference paradigm for dramatically scaling the reasoning power of LLMs. The key insight is that reasoning to generate computations, computations that do tasks or find answers or reveal information, gives rise to meta-reasoning. This behavior sheds the failures plaguing ReAct while keeping all its benefits, offers additional benefits, and, we believe, carries a far higher ceiling for reasoning LLMs. We define meta-reasoning as the behavior of identifying problems, reasoning out the computation that solves them, and assimilating its work. A PLM is made of two parts: the harness, where computations are authored and run as policies, and the model, trained to reason them out.</p> <p>What an RLM does for its input/output, a PLM does for all its computation. Instead of reasoning and computing inline, a PLM authors policies: programmatic artifacts it can run, edit, and compose, and lets them carry the work a trajectory would otherwise hold: the mechanical steps, the reasoning, the interaction with the world. Policies live and execute in a persistent REPL environment. Our harness also provides sub-LLM functional policies, so a PLM can create policies capable of reasoning and computation. The PLM can control what a policy returns: our harness’s constraint abstraction lets it fix the invariants of any policy’s output, the sub-LLM policies included. The REPL is the only way a PLM acts; it is, however, not confined to only authoring policies there; it can inspect, compute, and act directly, much as an RLM does. What makes it a meta-reasoner is not a prohibition on doing the work itself but a disposition, trained into it, to author policies that do the work better than it would inline.</p> <p>The trajectory itself is responsible for abstract reasoning. There the PLM reasons over the goal and authors the policies that set its plans in motion: policies to test its hypotheses, policies to process inputs, policies to write and output code. A PLM’s job is not to run systems but to reason; it engineers its instruments with an understanding of the goal, sees the work they did, and reasons onward. And since reading an input is itself computation, the RLM capability comes along as a special case, a long input can sit in the environment and be worked through with policies.</p> <div class="text-center"> <img src="/assets/img/posts/plm-evolab.png" alt="A PLM authoring an evolutionary policy" style="max-width: 100%;"/> </div> <p class="text-center" style="font-size: 0.9em; color: #6b7280;"><em>A PLM reasons about the goal abstractly and expresses the computation as policies. Here, the PLM authors a single evolutionary policy that breeds candidate policies through sub-LLM builders, selects among them by played outcomes, and folds only gated winners into its deliverable.</em></p> <p>What the substrate buys is compounding. A policy authored once can be rerun, revised, and composed into larger ones, so the work of reasoning accumulates as artifacts rather than as tokens. The substrate alone, however, does not produce the behavior. In our experiments, current LLMs placed on the PLM harness do not spontaneously meta-reason, they grind the work inline out of habit, or scatter one-off delegations that are at best half-cooked. The disposition has to be trained in. We take that as the finding that matters: meta-reasoning is a trainable surface of improvement that today’s models have never been optimized for.</p> <h2 id="the-harness">The harness</h2> <p>A Policy Language Model is a large language model that meta-reasons: it reasons about the goal on its own trajectory and expresses the computation as policies in a persistent programming environment \(\mathcal{E}\). Each round the model thinks abstractly, then acts through one code tool call that executes in \(\mathcal{E}\) and updates its state. A PLM trajectory holds only three things: the abstract reasoning, the code, and the code’s output as the tool result.</p> <p>Meta-reasoning is the axis of inference along which the model reasons about the goal and reasons about generating the computations that will help attain it. It is three abilities held together: the ability to reason abstractly about the goal; the ability to reason and generate optimal compute structures for tasks; and the behavior that binds the two, in which the model reasons about what to do and how to do it, inlines or expresses the task as computation, benefits from its result, and reasons on.</p> <p>A policy is any computation the PLM expresses rather than performs. The definition is behavioral, not syntactic, and the harness gives it support: the <code class="language-plaintext highlighter-rouge">@policy</code> decorator turns a function or a class into a named artifact that persists across rounds, holds global scope so it is used by name and never imported, and can be edited in place and duplicated. Authored once, it can be rerun, edited, and composed into larger policies.</p> <p>The harness gives the PLM three LLM policies to build with: <code class="language-plaintext highlighter-rouge">natural_llm</code> is a single llm call; <code class="language-plaintext highlighter-rouge">react_llm</code> is an LLM with the python tool, run in ReAct rounds; and <code class="language-plaintext highlighter-rouge">react_verifier_llm</code> is a special <code class="language-plaintext highlighter-rouge">react_llm</code> with a policy that runs after each round to manipulate the llm’s trajectory. Bare minimum on their own, they can be used to build arbitrarily complex information processing circuits. The building, however, is deliberate work: every sub-llm call begins empty. Every input, helper, prompt, any policy it will use, the <code class="language-plaintext highlighter-rouge">@policy</code> decorator and the LLM policies themselves, are handed to it. Nothing about a sub-llm is ambient, so a working one is constructed, not summoned. Fanning out is never a reflex but a decision the PLM has to make and build, so it decomposes only when it decides to. And this costs no expressive power: a PLM can build an RLM, or even an unbounded-depth PLM, as a policy inside its depth-two harness. Computation can never be fully controlled or predicted, but a harness decides who holds what control there is. Ours concentrates it in the PLM. An RLM scatters it across the tree.</p> <p>Constraints and message weaving complete the harness. A constraint is a way to define invariants over a computation’s output, authored as a typed class. An LLM’s judgment so becomes a typed value in ordinary control flow, <code class="language-plaintext highlighter-rouge">while react_llm("...", constraint=Bool)</code> either runs or raises, and there is no third outcome. Message weaving makes trajectories the same kind of object: messages pass by reference, so a <code class="language-plaintext highlighter-rouge">react_llm</code> run over a list leaves its turns on that list, and the list can be handed onward as an object, to a second policy that inspects the reasoning, never as pasted text; the PLM’s own trajectory is available the same way, as a live copy.</p> <h2 id="what-has-to-be-trained">What has to be trained</h2> <p>Placed on the harness untrained, current models delegate but do not meta-reason, and the affinity improves little under prompting, even with detailed, optimized system prompts, reasoning models hold to the habits of the single trajectory. The disposition has to be trained in, and the training is not only subtractive. Taking work off the trajectory is half of it, the other half is new duties, authoring, granting, briefing, weaving, that no model has ever been trained to perform. How meta-reasoning is enforced, how it is rewarded, how it is discovered at all, are open questions, and we leave them open deliberately. The substrate is more powerful than the one it is compared to, and that is exactly why it must be trained. Developing that training is the program of this work.</p> <hr/> <p><em>The paper is in progress; a read-only draft is available <a href="https://www.overleaf.com/read/npgkvjmftsyj#aeb894">here</a>.</em></p>]]></content><author><name></name></author><category term="research"/><category term="llm"/><category term="meta-reasoning"/><summary type="html"><![CDATA[A complementary axis of inference-time scaling that biases the LLM toward abstract computational reasoning. What an RLM does for its input, a PLM does for all its computation.]]></summary></entry><entry><title type="html">Forecasting</title><link href="https://ddi-droid.github.io/blog/2026/forecasting-season/" rel="alternate" type="text/html" title="Forecasting"/><published>2026-01-16T12:00:00+00:00</published><updated>2026-01-16T12:00:00+00:00</updated><id>https://ddi-droid.github.io/blog/2026/forecasting-season</id><content type="html" xml:base="https://ddi-droid.github.io/blog/2026/forecasting-season/"><![CDATA[<p>For the first half of 2026, a good part of our work at Kairosity went into competitive forecasting. It is one of the few tasks where the world itself grades your reasoning, on a schedule, with no partial credit, and frontier models have been closing the gap to human superforecasters fast enough that the competitions got interesting. We entered four. This post is about what we built for each and what the season taught us.</p> <h2 id="the-arenas">The arenas</h2> <p><strong>The Metaculus AI Benchmark, Spring 2026.</strong> A bot-only tournament, a few hundred questions over the season, log-scored against how the world resolves, with the bots benchmarked against the Metaculus community’s own predictions. Alongside it runs MiniBench, back-to-back two-week sprints of about sixty auto-resolving questions, which we used as a fast feedback loop.</p> <p><strong>ProphetHacks.</strong> A 32-hour hackathon out of UChicago in May, run by Sigma Lab, Fleet AI, and Kalshi. You ship an agent that predicts on live Kalshi prediction-market events, and the leaderboard ranks agents by live PnL over a two-week evaluation window. Nothing concentrates the mind like a leaderboard that re-ranks every fifteen seconds.</p> <p><strong>Prophet Arena.</strong> The live benchmark from the LLM-as-a-Prophet group, which continuously evaluates forecasters on real prediction-market events, Brier-scored, across multiple horizons. After the hackathon we kept an agent standing there.</p> <p><strong>The Upstart Macro Index competition.</strong> The quiet one. Monthly forecasts of a macroeconomic index, judged over months rather than minutes.</p> <h2 id="metaculus-or-the-committee-that-writes-code">Metaculus, or the committee that writes code</h2> <p>Our Metaculus bot is a committee of three researchers with one editor, and the rule that defines it is that the researchers are not allowed to hand-wave. Each question first goes through iterative research. A search agent pulls from news and web sources through a single gateway, an analyst critiques what came back, and a supervisor either declares the evidence sufficient or emits targeted sub-queries for another round.</p> <p>Then three researchers attack the question in parallel, each deliberately running on a different model, so their mistakes decorrelate. Each researcher must anchor on prediction-market and community priors where they exist, and downweight thin ones. Each must state a base rate before updating on news. And each must express its final model as Python. Real scipy distributions, Monte Carlo with at least ten thousand samples, no magic numbers, every constant tagged as researched or assumed. The code actually runs. If <code class="language-plaintext highlighter-rouge">predict()</code> fails or returns the wrong outcome keys, the error goes back to the researcher and it tries again.</p> <p>A consensus agent then reads all three, scores each on analysis quality and model soundness, assigns weights, is allowed to throw a researcher out entirely, fixes unit mistakes (a researcher who says “75” when the question wants 75,000,000 gets corrected, not trusted), and takes the weighted average. Before anything is submitted, there is one last discipline, an overconfidence tax applied exactly once:</p> \[X = \min(10 - c,\ 0.2\,p)\] <p>where \(c\) is the researcher’s own certainty out of ten and \(p\) is the leaning-side estimate in percent, and the estimate gets shaved by \(X\) toward the middle. The rule comes with a clause we consider load-bearing: you are not allowed to tune \(c\) to keep the number you already wanted.</p> <p>The other half of the Metaculus story is operations. The bot runs every twenty minutes, first from CI, then from a server with cron, tmux, and a lockfile so runs never overlap. Every external API sits behind a dispatcher that enforces per-service rate limits with adaptive backoff, because Metaculus wants one request every four seconds and news APIs want less than that. And we have the war story that justifies all of it. One night the dispatcher process died at 03:20 UTC. Cron kept firing every twenty minutes, each run hit connection refused, and by 04:40 the question had closed without our forecast. The forecasting was fine. The plumbing lost the points. In live tournaments, reliability is not an engineering nicety, it is a scoring term.</p> <h2 id="prophethacks-or-32-hours-and-a-bot-called-greeniebot">ProphetHacks, or 32 hours and a bot called GreenieBot</h2> <p>Before writing any code for the hackathon, we studied the field. Metaculus bots post their reasoning publicly, and we had been reading the traces of a strong one called GreenieBot. We pulled its public traces, distilled them into two lists, behaviors to copy and behaviors to avoid, and baked both into our prompts as few-shot examples. Some of those lessons are still in our prompt files verbatim, like the reminder that models systematically underprice questions where a benchmark score is already near the threshold, and the line we quote at each other: overweighting formal caution can be as damaging as overconfidence.</p> <p>The hackathon agent itself is small and paranoid. One HTTP endpoint. Retrieval is bounded Exa search planned by a cheap model, two rounds, a handful of queries per round, at most ten sources, under a hard 420-second wall clock. Forecasting is two independent forecaster-verifier branches running in parallel, the verifier hunting resolver mistakes, prior misuse, and overconfidence, and a synthesizer that merges the branches without ever smearing to uniform. And if anything anywhere throws, the agent returns a uniform fallback rather than nothing, because completion rate is scored and a missed submission is worse than an honest coin flip. The line from our submission notes: a beautiful forecast that misses the schema is still a failed forecast.</p> <p>One Kalshi-specific detail cost us real thought. Listed outcomes on a market do not have to sum to one, the resolver semantics are per-label, so the agent preserves calibrated per-label probabilities and refuses to normalize unless the resolver says the outcomes are exhaustive and exclusive. It is exactly the kind of detail that silently costs points if your pipeline helpfully normalizes behind your back.</p> <p>On the live PnL leaderboard we sat first for a moment. We ended third.</p> <h2 id="prophet-arena-or-staying-live">Prophet Arena, or staying live</h2> <p>After the hackathon we took the same core, scrubbed it into a clean deployment, and kept it standing on Prophet Arena, where events keep arriving from real markets and the grading never stops. The engineering brief changes when there is no closing ceremony. Under load testing, three parallel requests complete in about 39 seconds each with both forecast branches running, and the whole pipeline stays comfortably under the ten-minute budget per question. Same philosophy as the hackathon, calibration over confidence, schema over beauty, always answer.</p> <h2 id="upstart-or-knowing-when-not-to-send-an-agent">Upstart, or knowing when not to send an agent</h2> <p>The Upstart Macro Index competition asked for monthly index forecasts with uncertainty bands. We tracked the index’s regime, fit AR(1) models on the recent window, and bootstrapped the intervals. The index peaked around 1.67 in late 2023 and read 1.39 in December 2025, with California diverging well below the national line, and our submissions came down to reading monetary policy and sector layoffs correctly, then letting a two-parameter model from a statistics textbook do the arithmetic. Knowing when the right forecaster is not an agent is also a forecasting skill.</p> <h2 id="what-carried-over">What carried over</h2> <p>Across four arenas, the same three lessons kept re-proving themselves.</p> <p>Reliability is a scoring term. Fallbacks, rate limits, lockfiles, timeouts, completion rate. Nearly every point we lost all season was lost by plumbing, not by reasoning.</p> <p>Calibration wants machinery, not vibes. The explicit shrink formula, verifiers whose job is to attack overconfidence, market priors as anchors, unit-checking before aggregation. Every one of these exists because we watched a raw model be confidently wrong in a specific, repeatable way.</p> <p>And the one that became a research question. Ask a strong model the same question in different clothes, a binary here, the same thing as a multiple choice there, and the numbers stop agreeing with each other. Accuracy kept improving all season. Consistency did not. That observation turned into our working paper, <a href="https://openreview.net/forum?id=Z51qVtmHAv">Brittle Prophets</a>, where we test whether a causal world model can actually fix this.</p>]]></content><author><name></name></author><category term="research"/><category term="forecasting"/><category term="agents"/><summary type="html"><![CDATA[The competitions we entered at Kairosity, the agents we built for them, and what live leaderboards taught us about reliability, calibration, and consistency.]]></summary></entry><entry><title type="html">AFramework</title><link href="https://ddi-droid.github.io/blog/2025/aframework/" rel="alternate" type="text/html" title="AFramework"/><published>2025-11-16T12:00:00+00:00</published><updated>2025-11-16T12:00:00+00:00</updated><id>https://ddi-droid.github.io/blog/2025/aframework</id><content type="html" xml:base="https://ddi-droid.github.io/blog/2025/aframework/"><![CDATA[<div class="text-center"> <img src="/assets/img/posts/aframework-logo.png" alt="AFramework" style="max-width: 300px;"/> </div> <p>AFramework is an agent framework, that is what the A stands for. I built it at Kairosity, and this is its open-source version. The idea fits in one line: separate the applications that use LLM inference from the inference itself.</p> <h2 id="why">Why</h2> <p>Picture one machine’s worth of CPU and RAM, and many applications that all want LLM-driven work done on it. The GPU side of this separation already has its answer: engines like vLLM and SGLang sit between the model and everyone who wants a forward pass, and schedule those passes well. But an agentic workload is not a forward pass. A ReAct-style task is long stretches of I/O, tool calls, API waits, retrieval, braided with bursts of CPU work, and the model call is only one piece of it. For that side of inference there is no shared substrate, so every application builds its own orchestration, its own rate limiting, its own retries, around the same recurring needs.</p> <p>AFramework is that substrate. An application submits a task over a socket and stops caring. A shared daemon runs it, next to every other application’s tasks, and optimizes the whole load on whatever the box has.</p> <p>There is also a blunter reason. The popular frameworks in this space break often, LangChain most famously, and the design goal here was the opposite of a framework that does everything: a small daemon with a narrow contract that stays up.</p> <h2 id="the-shape-of-the-system">The shape of the system</h2> <p>You do not start AFramework. The first client call that needs it starts the daemon on a Unix socket if one is not already answering, and plugin modules load lazily, only the ones your spec names. For a long-lived deployment you can run the daemon yourself, but nothing requires it.</p> <p>Work runs in isolated worker processes, real parallelism rather than one event loop pretending. A supervisor health-checks the workers, respawns the dead ones, and drains them on shutdown.</p> <p>Behavior lives in agent cores. The default core runs a reasoning loop with a model backend and a verifier attached, and custom cores define their own contract with the worker. Everything is specified as plain dicts naming a class, backends are swappable, vLLM, OpenAI, Anthropic, and MCP servers are first class.</p> <p>The verifier is worth a word, because the name undersells it. Think of it as a monitor that runs over the reasoning each round. It can be an agent in its own right, an LLM that reads the trajectory so far and decides whether the loop should keep going, or it can be a plain condition that fires at a set point: summarize the context once it crosses a token threshold, stop when a criterion is met, structure the final answer before it leaves. It is the hook for watching a reasoning loop while it runs, not only reading what it produced.</p> <p>Results are durable. A client can submit a task, die, and a different client can later ask for the result and get the same answer, or the same error. Errors are fail-fast and typed: a failing task marks itself failed with a structured error payload, its coroutine dies alone, and the worker keeps serving everything else.</p> <h2 id="keeping-many-apps-honest">Keeping many apps honest</h2> <p>Sharing a box is a promise applications break on each other, so enforcement is central rather than per-app. Rate limits are Redis-backed token buckets on named resource keys, with circuit breakers over failing resources, and policies declared in one YAML file. Resource keys can be tenant-qualified, <code class="language-plaintext highlighter-rouge">acme.openai:gpt-4</code> style, so “many applications” can also mean “many tenants” without one of them eating the budget of the rest.</p> <h2 id="the-router">The router</h2> <p>Somebody has to decide which worker a task lands on, and round-robin is the answer most systems settle for. AFramework routes by cost. For each candidate worker it estimates the expected latency of this task on that worker, from the worker’s projected CPU and I/O load, with memory pressure and event-loop lag as hard constraints, and a warm-start bonus when a worker has recently run tasks of the same kind. On large fleets it samples a few candidates, power-of-d-choices; on small fleets it simply scans them all.</p> <p>The part I like most: task costs are not configured anywhere. They are learned online, per task signature, with Bayesian changepoint detection watching for the moment a task’s behavior shifts regime, so the profile follows the workload instead of trusting old evidence. The router deserves a post of its own, so this section is deliberately the short version.</p> <h2 id="the-c-underneath">The C++ underneath</h2> <p>The hot path is C++ where it matters, the dispatch queues between router and workers (single-producer, multi-consumer), the scheduler and its worker topology, and the logger’s wire service, each with a Python fallback so the framework still runs unbuilt, just slower. The reasoning is boring and firm: dispatch overhead is paid by every task that moves, so the dispatch path is the one place that must never be the bottleneck.</p> <h2 id="using-it">Using it</h2> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">asyncio</span>
<span class="kn">from</span> <span class="n">AFramework.agent_core</span> <span class="kn">import</span> <span class="n">BaseAgentCoreClient</span>

<span class="k">async</span> <span class="k">def</span> <span class="nf">main</span><span class="p">():</span>
    <span class="n">model_backend</span> <span class="o">=</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">model_backend_class_name</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">VLLMBackend</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">model</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Qwen/Qwen3-30B-A3B-Thinking-2507</span><span class="sh">"</span><span class="p">,</span>
    <span class="p">}</span>
    <span class="n">client</span> <span class="o">=</span> <span class="nc">BaseAgentCoreClient</span><span class="p">(</span><span class="n">agent_id</span><span class="o">=</span><span class="sh">"</span><span class="s">my_agent</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">client</span><span class="p">.</span><span class="nf">register_model_backend</span><span class="p">(</span><span class="o">**</span><span class="n">model_backend</span><span class="p">)</span>
    <span class="n">client</span><span class="p">.</span><span class="nf">register_verifier</span><span class="p">(</span>
        <span class="n">verifier_class_name</span><span class="o">=</span><span class="sh">"</span><span class="s">BaseVerifier</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">model_backend</span><span class="o">=</span><span class="n">model_backend</span><span class="p">,</span>
        <span class="n">max_verification_steps</span><span class="o">=</span><span class="mi">20</span><span class="p">,</span>
    <span class="p">)</span>

    <span class="n">session_id</span> <span class="o">=</span> <span class="k">await</span> <span class="n">client</span><span class="p">.</span><span class="nf">execute_task</span><span class="p">(</span>
        <span class="n">messages</span><span class="o">=</span><span class="p">[{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Hello world</span><span class="sh">"</span><span class="p">}],</span>
        <span class="n">stream</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
    <span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Task started with Session ID: </span><span class="si">{</span><span class="n">session_id</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>

<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="n">asyncio</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span><span class="nf">main</span><span class="p">())</span>
</code></pre></div></div> <p>That is the whole ceremony. No daemon management, no service files, the framework comes up behind the first call and the task runs in a worker with routing, rate limits, and durability underneath it.</p> <h2 id="extending-it">Extending it</h2> <p>The default core is one implementation of a contract, not the only one. Your own agent core is a class behind a decorator:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">AFramework</span> <span class="kn">import</span> <span class="n">agent_core</span><span class="p">,</span> <span class="n">BaseAgentCore</span>

<span class="nd">@agent_core</span>
<span class="k">class</span> <span class="nc">MyAgentCore</span><span class="p">(</span><span class="n">BaseAgentCore</span><span class="p">):</span>
    <span class="c1"># implement register_task / execute_task; define your own loop
</span>    <span class="bp">...</span>
</code></pre></div></div> <p>The decorator registers the class by its name, and workers load it lazily when a task asks for it. Tools register the same way, a function behind a hook:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">AFramework</span> <span class="kn">import</span> <span class="n">tool_hook</span>

<span class="nd">@tool_hook</span>
<span class="k">async</span> <span class="k">def</span> <span class="nf">register_my_tools</span><span class="p">(</span><span class="n">registry</span><span class="p">):</span>
    <span class="k">await</span> <span class="n">registry</span><span class="p">.</span><span class="nf">register_function</span><span class="p">(</span><span class="sh">"</span><span class="s">my_tool</span><span class="sh">"</span><span class="p">,</span> <span class="n">my_tool_fn</span><span class="p">,</span> <span class="sh">"</span><span class="s">Description</span><span class="sh">"</span><span class="p">,</span> <span class="p">{})</span>
</code></pre></div></div> <p>Model backends and verifiers carry the same <code class="language-plaintext highlighter-rouge">@model_backend</code> and <code class="language-plaintext highlighter-rouge">@verifier</code> decorators. Everything in the system is a plugin sitting behind a name, which is what lets a task spec be nothing but plain dicts naming the pieces it wants.</p> <h2 id="a-small-example">A small example</h2> <p>Here is the shape a real app takes: an agent that can factor numbers, because a model is bad at arithmetic and a tool is exact. The tool is an ordinary Python function.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># tools.py
</span><span class="k">def</span> <span class="nf">factorize</span><span class="p">(</span><span class="n">n</span><span class="p">:</span> <span class="nb">int</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">list</span><span class="p">[</span><span class="nb">int</span><span class="p">]:</span>
    <span class="sh">"""</span><span class="s">Return the prime factors of n.</span><span class="sh">"""</span>
    <span class="n">factors</span><span class="p">,</span> <span class="n">d</span> <span class="o">=</span> <span class="p">[],</span> <span class="mi">2</span>
    <span class="k">while</span> <span class="n">d</span> <span class="o">*</span> <span class="n">d</span> <span class="o">&lt;=</span> <span class="n">n</span><span class="p">:</span>
        <span class="k">while</span> <span class="n">n</span> <span class="o">%</span> <span class="n">d</span> <span class="o">==</span> <span class="mi">0</span><span class="p">:</span>
            <span class="n">factors</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">d</span><span class="p">)</span>
            <span class="n">n</span> <span class="o">//=</span> <span class="n">d</span>
        <span class="n">d</span> <span class="o">+=</span> <span class="mi">1</span>
    <span class="k">if</span> <span class="n">n</span> <span class="o">&gt;</span> <span class="mi">1</span><span class="p">:</span>
        <span class="n">factors</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">n</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">factors</span>
</code></pre></div></div> <p>The app wires a backend, a verifier, and that tool onto a client, then asks a question and waits for the answer.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">asyncio</span>
<span class="kn">from</span> <span class="n">AFramework.agent_core</span> <span class="kn">import</span> <span class="n">BaseAgentCoreClient</span>

<span class="n">model_backend</span> <span class="o">=</span> <span class="p">{</span>
    <span class="sh">"</span><span class="s">model_backend_class_name</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">OpenAIBackend</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">model</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">gpt-4o</span><span class="sh">"</span><span class="p">,</span>
<span class="p">}</span>

<span class="k">async</span> <span class="k">def</span> <span class="nf">main</span><span class="p">():</span>
    <span class="n">client</span> <span class="o">=</span> <span class="nc">BaseAgentCoreClient</span><span class="p">(</span><span class="n">agent_id</span><span class="o">=</span><span class="sh">"</span><span class="s">factorizer</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">client</span><span class="p">.</span><span class="nf">register_model_backend</span><span class="p">(</span><span class="o">**</span><span class="n">model_backend</span><span class="p">)</span>
    <span class="n">client</span><span class="p">.</span><span class="nf">register_verifier</span><span class="p">(</span>
        <span class="n">verifier_class_name</span><span class="o">=</span><span class="sh">"</span><span class="s">BaseVerifier</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">model_backend</span><span class="o">=</span><span class="n">model_backend</span><span class="p">,</span>
        <span class="n">max_verification_steps</span><span class="o">=</span><span class="mi">10</span><span class="p">,</span>
    <span class="p">)</span>
    <span class="n">client</span><span class="p">.</span><span class="nf">register_tool_spec</span><span class="p">(</span>
        <span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">factorize</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">import_module</span><span class="o">=</span><span class="sh">"</span><span class="s">tools</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">attr</span><span class="o">=</span><span class="sh">"</span><span class="s">factorize</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">description</span><span class="o">=</span><span class="sh">"</span><span class="s">Return the prime factors of an integer n.</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">parameters</span><span class="o">=</span><span class="p">{</span>
            <span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">object</span><span class="sh">"</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">properties</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span><span class="sh">"</span><span class="s">n</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span><span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">integer</span><span class="sh">"</span><span class="p">}},</span>
            <span class="sh">"</span><span class="s">required</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span><span class="sh">"</span><span class="s">n</span><span class="sh">"</span><span class="p">],</span>
        <span class="p">},</span>
    <span class="p">)</span>

    <span class="n">result</span> <span class="o">=</span> <span class="k">await</span> <span class="n">client</span><span class="p">.</span><span class="nf">execute</span><span class="p">(</span>
        <span class="n">messages</span><span class="o">=</span><span class="p">[{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">What are the prime factors of 1001? Use the tool.</span><span class="sh">"</span><span class="p">}],</span>
    <span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="n">result</span><span class="p">[</span><span class="sh">"</span><span class="s">task_result</span><span class="sh">"</span><span class="p">])</span>

<span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="n">asyncio</span><span class="p">.</span><span class="nf">run</span><span class="p">(</span><span class="nf">main</span><span class="p">())</span>
</code></pre></div></div> <p>Nothing here starts a server or manages a process. The daemon comes up on the first call, the worker registers the tool from its import path, the agent calls it, the verifier watches the loop, and the answer comes back. The application only ever described what it wanted.</p> <p>AFramework is open source on <a href="https://github.com/Curiosity-Oneiroi/AFramework">GitHub</a>.</p>]]></content><author><name></name></author><category term="engineering"/><category term="systems"/><category term="llm"/><category term="agents"/><summary type="html"><![CDATA[Why I built an agent framework that separates applications from LLM inference, and a tour of how it works.]]></summary></entry><entry><title type="html">PKS: We thought this was hard</title><link href="https://ddi-droid.github.io/blog/2025/possible-kemeny-score/" rel="alternate" type="text/html" title="PKS: We thought this was hard"/><published>2025-09-11T12:00:00+00:00</published><updated>2025-09-11T12:00:00+00:00</updated><id>https://ddi-droid.github.io/blog/2025/possible-kemeny-score</id><content type="html" xml:base="https://ddi-droid.github.io/blog/2025/possible-kemeny-score/"><![CDATA[<p>This post is about one section of our paper, <a href="https://arxiv.org/abs/2603.28574">Bribery’s Influence on Ranked Aggregation</a>. The paper studies what happens to the Kemeny score under manipulation, bribery, deleting rankings, deleting candidates, and so on. This post is about a problem we were stuck on the longest, a problem called <strong>Possible Kemeny Score</strong>, which resisted us for a while and then fell to an algorithm so simple we didn’t believe it at first.</p> <h2 id="rankings-and-disagreements">Rankings and disagreements</h2> <p>Rank aggregation shows up everywhere. Search engines merge ranked result lists, recommendation systems merge preference orders, panels of judges merge scorecards. The classical way to merge rankings is <strong>Kemeny’s rule</strong>, from 1959. Given a set of rankings, find the single ranking that disagrees with them the least. It has a nice statistical story too. If voters are noisy observers of one true ranking, the Kemeny ranking is the maximum likelihood estimate of that truth.</p> <p>“Disagrees the least” is measured by the <strong>Kendall tau distance</strong>. For two rankings \(\pi\) and \(\pi'\) over the same candidates, it counts the pairs ordered differently:</p> \[\mathtt{KT}(\pi,\pi') = \big|\{\,\{x,y\} : x \prec_{\pi} y \ \wedge\ y \prec_{\pi'} x \,\}\big|\] <p>If you draw the two rankings side by side and connect each candidate to itself, the distance is just the number of crossings.</p> <div class="text-center"> <img src="/assets/img/posts/kemeny-crossings.svg" alt="Two rankings with connecting lines; crossings marked" style="max-width: 100%;"/> </div> <p>The distance from one ranking \(X\) to a whole set of rankings \(\mathcal{R}\) is the sum, \(\mathtt{KT}(X,\mathcal{R}) = \sum_{R\in\mathcal{R}}\mathtt{KT}(X,R)\).</p> <h2 id="the-problem-intuitively">The problem (intuitively)</h2> <p>Real ballots are incomplete. A reviewer ranks the five papers they actually read. A user ranks the movies they have seen. So suppose the rankings you are given are partial, and suppose someone hands you a target ranking \(X\) over all the candidates and a number \(k\), and asks:</p> <p><strong>Possible Kemeny Score.</strong> Given a candidate set \(\mathcal{C}\), a set of partial rankings \(\mathcal{R}\), a full ranking \(X\), and an integer \(k\), can the partial rankings be completed so that the total Kendall tau distance of \(X\) from the completed rankings is at most \(k\)?</p> <p>Completing a ranking means inserting the missing candidates anywhere you like, without touching the relative order of the candidates already ranked.</p> <div class="text-center"> <img src="/assets/img/posts/kemeny-problem.svg" alt="A partial ballot with missing candidates waiting to be inserted" style="max-width: 100%;"/> </div> <p>In other words, is there a completion of the evidence under which \(X\) scores at most \(k\)? It is the optimist’s question, and it is also the auditor’s question. Given incomplete preference data, what is the best score this target could possibly have?</p> <h2 id="why-we-thought-it-was-hard">Why we thought it was hard</h2> <p>Everything in this problem’s neighborhood is hard. Computing the Kemeny score is NP-complete (Bartholdi, Tovey &amp; Trick, 1989). It is NP-hard already for four rankings, and the case of three rankings has been open for years (Dwork, Kumar, Naor &amp; Sivakumar, 2001; Biedl, Brandenburg &amp; Deng, 2009). The corresponding manipulation questions for Kemeny <em>Consensus</em> are coNP-complete and \(\Sigma_2^p\)-complete (Fitzsimmons &amp; Hemaspaandra, 2021). And the most widely studied problem of this shape, the Possible Winner problem, where partial votes are completed to make a favorite candidate win, is NP-hard for most common voting rules (Xia &amp; Conitzer, 2011). All of this pointed at the same thing: the problem is hard. Or so we thought.</p> <p>But our stuckness was more concrete than that. Try to solve it. You have to insert the missing candidates into a ballot, and there are two levels of freedom, and both looked unmanageable.</p> <p>First level. In what relative order do the missing candidates go in? You might guess “in the order \(X\) ranks them,” but we had no reason to believe that. The candidates already sitting in the ballot do not agree with \(X\), they are in whatever order the voter put them. So maybe following \(X\)’s order among the new candidates generates extra disagreements against those already-placed candidates, and some other order does better. We could not rule that out, and without it we had nothing to hold on to.</p> <p>Second level. Even if you somehow knew the right relative order, where do the candidates actually go? Place the first one at some position. Nothing guarantees that position is consistent with any optimal completion. It felt like every placement decision could poison the ones after it, which is exactly the situation where you expect to need search, and search over interleavings is exponential.</p> <p>We could not find an approach.</p> <h2 id="the-first-insight">The first insight</h2> <p>The first insight was that the natural guess turns out to be right. In any optimal completion, the inserted candidates appear in \(X\)’s order relative to each other.</p> <p><strong>Lemma 1.</strong> <em>Let \(\hat{R}\) be an optimal extension of \(R\). Then \(\hat{R}\) and \(X\) agree over the inserted candidates.</em></p> <p>The proof is an exchange argument. Suppose two inserted candidates \(c, c'\) sit in the wrong order, \(c \prec_X c'\) but \(c' \prec_{\hat{R}} c\). Swap them. The pair \(\{c,c'\}\) itself now agrees with \(X\), so that is one disagreement gone. For any candidate \(w\) caught between them, check the three places \(w\) can sit in \(X\): before both, between them, after both. Walking the cases, every disagreement the swap creates is matched by one it removes, and the between case removes two. The distance never goes up, and the pair itself strictly improves. So an out-of-order pair can never be in an optimal completion.</p> <div class="text-center"> <img src="/assets/img/posts/kemeny-swap.svg" alt="Swapping an out-of-order pair reduces disagreements from 4 to 1" style="max-width: 100%;"/> </div> <p>This kills the first level of freedom completely. The internal order of the insertions is not a choice, it is \(X\)’s order. What remains is only the question of positions.</p> <h2 id="the-second-insight-and-the-algorithm">The second insight, and the algorithm</h2> <p>The position problem was still standing. Insert the candidates in \(X\)’s order, fine, but put the first one somewhere and you still have no guarantee that any optimal completion has it there.</p> <p>The algorithm that resolves it looks like this.</p> <div class="text-center"> <img src="/assets/img/posts/pks-algorithm.png" alt="Algorithm 1: Possible-Kemeny-Score" style="max-width: 100%;"/> </div> <p>Take the missing candidates in \(X\)’s order. Insert each one at the <strong>leftmost</strong> position that minimizes its disagreements with the already-ranked candidates. And after you place a candidate, never look to the left of it again, the pointer \(l\) only moves right.</p> <p>A small example. Let \(X = a \prec b \prec c \prec d \prec e\) and let the ballot be \(R = c \prec a \prec e\), so \(b\) and \(d\) are missing and get inserted in that order.</p> <p>For \(b\), the four available slots cost \(1, 2, 1, 2\) disagreements against the fixed candidates. Positions \(0\) and \(2\) tie, so the leftmost wins and \(b\) goes first: \(b \prec c \prec a \prec e\). For \(d\), the slots from the pointer onward cost \(2, 1, 0, 1\), so \(d\) goes after \(a\): \(b \prec c \prec a \prec d \prec e\). Total distance to \(X\): the ballot’s own inversion \((c,a)\), plus \(1\) for \(b\), plus \(0\) for \(d\). You can check by brute force that no completion of \(R\) does better.</p> <h2 id="why-it-works">Why it works</h2> <p>Two facts carry the proof.</p> <p>The first is a monotonicity that we did not expect. Suppose a placed candidate \(\Pi(i)\) does at least as well further right, at some position \(t_f\) that minimizes its disagreements among the positions after it. Then you can push it there, dragging along every later insertion sitting in between, and never pay for it. The engine is a pointwise inequality. When \(\Pi(i)\) moves one step right past a fixed candidate, the change in its disagreements with the fixed candidates is \(+1\) or \(-1\). And for every later insertion \(\Pi(\ell)\), with \(\ell &gt; i\),</p> \[\Delta\,\Pi(\ell) \;\leq\; \Delta\,\Pi(i).\] <p>The reason is the agreement from Lemma 1. If the step saves a disagreement for \(\Pi(i)\), the fixed candidate it passes comes before \(\Pi(i)\) in \(X\), and since \(\Pi(i) \prec_X \Pi(\ell)\), it also comes before \(\Pi(\ell)\), so the same step saves one for \(\Pi(\ell)\) too. The later candidates ride along for free. Summed over the whole journey to \(t_f\), nobody’s total goes up.</p> <div class="text-center"> <img src="/assets/img/posts/pks-figure1.jpg" alt="Figure 1 from the paper: moving Pi(i) from t_i to t_f" style="max-width: 100%;"/> </div> <p>The second fact is the induction that turns this into optimality. At every step of the algorithm there exists an optimal completion that agrees with everything placed so far. If the optimal completion has the current candidate further left than the algorithm put it, move it right using the free-ride lemma. If it has it further right, moving it left to the algorithm’s position costs nothing, because the algorithm’s position was a minimizer. Either way there is an optimal completion matching the algorithm’s prefix, and at the end, matching its entire output.</p> <p>Ballots do not interact, the total distance is a sum, so running the procedure on each ballot independently is optimal overall. The whole thing runs in \(\mathcal{O}(n \cdot m^3)\), for \(n\) ballots over \(m\) candidates.</p> <h2 id="in-hindsight">In hindsight</h2> <p>For a long time we were convinced this problem was hard, and the honest reason is that we had not found the two lemmas. Without the agreement lemma there is no order to insert in, and without the free-ride inequality no placement is safe to commit to. Both freedoms looked like they demanded search. The two symmetries we discovered are what make a greedy algorithm the solution.</p> <p>It is a strange table our paper ends up with. For Kemeny <em>Consensus</em>, these manipulation questions sit at coNP-complete and \(\Sigma_2^p\)-complete where their status is known at all. For Kemeny <em>Score</em>, the decision version, all of these turn out polynomial, bribery via a reduction to Knapsack, ranking deletion, swap bribery, and the problem in this post. Same rule, same manipulative actions, a complexity gulf between the optimisation and decision questions.</p> <p>The three-voter Kemeny score, whether it is polynomial or NP-hard, is still open, and so is every Kemeny Winner column of our table.</p> <hr/> <p><em>Read the full paper on <a href="https://arxiv.org/abs/2603.28574">arXiv</a>.</em></p>]]></content><author><name></name></author><category term="research"/><category term="social-choice"/><category term="algorithms"/><summary type="html"><![CDATA[How a problem we couldn't crack for weeks fell to a greedy algorithm, from our paper on manipulating the Kemeny score.]]></summary></entry><entry><title type="html">Fixing Table Bounding Boxes with RL</title><link href="https://ddi-droid.github.io/blog/2025/table-ocr-rl/" rel="alternate" type="text/html" title="Fixing Table Bounding Boxes with RL"/><published>2025-08-17T12:00:00+00:00</published><updated>2025-08-17T12:00:00+00:00</updated><id>https://ddi-droid.github.io/blog/2025/table-ocr-rl</id><content type="html" xml:base="https://ddi-droid.github.io/blog/2025/table-ocr-rl/"><![CDATA[<p>At Decimal Point Analytics I work with financial documents, and financial documents contain a lot of tables. This problem was plaguing us: our docket pipeline at the time genuinely struggled with tables. Detection comes first, a model draws bounding boxes around rows and cells, and everything downstream trusts those boxes. The boxes drift, rows merge, phantom rows appear, and long tables make it worse, by the bottom of one it gets hard to make sense of anything. Every bad box becomes a corrupted record two stages later.</p> <p>The usual fix is to fine-tune the detector with more labeled pages, and labeling table geometry is miserable work. So we tried a different route: keep the detector, and train a second model to repair its output. It did not start from scratch. The starting point was the prediction from Surya OCR, a very powerful OCR at the time, and the neural network learned to correct the mistakes in those bounding boxes.</p> <h2 id="the-idea">The idea</h2> <p>The environment is a page. The state is the detector’s boxes laid over the words it found. The network’s actions are small edits, nudge a box edge, delete a row box, halt when done. An episode starts from what Surya produced and ends with what the network left behind, and the reward measures whether the boxes ended up agreeing with the words on the page.</p> <p>That reward came in two designs. The first is geometric, computed from the page itself: how cleanly the row boxes cut between word clusters, how many words end up owned by exactly one row, whether a deleted row was one the words never needed. That version is implemented in the open code. The second design replaces geometry with judgment, an OpenAI vision model looks at the boxed page and scores it. The applied version went that way, and the honest note from the code’s own header is that it works even when the judge is somewhat jank.</p> <p>It worked a lot better than we expected, the repaired boxes showed real gains over the detector’s own, and a version of this went into use internally at DPA. The environments are C, built as PufferLib Ocean environments and trained with PufferLib’s PPO, which is what made iterating on a page-sized state cheap enough to try ideas like this at all.</p>]]></content><author><name></name></author><category term="research"/><category term="rl"/><category term="ocr"/><category term="systems"/><summary type="html"><![CDATA[Teaching a neural network to repair a table detector's bounding boxes, with geometric rewards in the open version and a vision model as the judge in the applied one.]]></summary></entry><entry><title type="html">PolyTM: learning polynomial-time oracles</title><link href="https://ddi-droid.github.io/blog/2025/polytm/" rel="alternate" type="text/html" title="PolyTM: learning polynomial-time oracles"/><published>2025-07-08T12:00:00+00:00</published><updated>2025-07-08T12:00:00+00:00</updated><id>https://ddi-droid.github.io/blog/2025/polytm</id><content type="html" xml:base="https://ddi-droid.github.io/blog/2025/polytm/"><![CDATA[<p>PolyTM is a reinforcement learning environment I built inside <a href="https://github.com/DDI-droid/PufferLib/tree/poly_tm">PufferLib</a>, around one question: can a neural network learn to discover algorithms? Not to answer a problem instance, but to produce the procedure that answers every instance, and to do it in polynomial time.</p> <h2 id="the-idea">The idea</h2> <p>The environment is a machine. There is a tape of integers holding the problem, and the agent does not output answers, it outputs instructions: pick two cells, pick an operation, add or multiply, pick a cell to write the result into. Step by step it edits the tape, and when it halts, the reward depends on whether the designated result cells hold the right values. The network is not being trained to know the answer. It is being trained to build the computation that produces the answer.</p> <p>The environment also keeps the two honest ledgers of computation: how many steps an episode took and how many cells it wrote. Running time and space, logged for every episode, because an algorithm that exists but wastes the tape is not the algorithm we are looking for.</p> <p>The committed version is this register machine. The version it was growing into is a proper multi-tape Turing machine, separate work, state, and result tapes, read heads with observation windows, an alphabet, penalties for refusing to halt. Same idea, stricter machine. The heads and their windows exist for a blunt reason, the observation vector cannot be the whole tape once the tape grows, so the agent reads through small windows around its heads and has to learn to move them, which is, of course, exactly what a Turing machine does. We experimented along that axis, more heads with narrow views against fewer heads with wider ones, because visibility was the budget everything else had to fit inside.</p> <p>The tasks across versions ran from summing and binary addition to sorting, each instance random every episode. A Sudoku solver was planned as the showpiece and never came through.</p> <p>The policy that drives it is a transformer over the tape. Each integer cell is read as its bytes, its sign, and a hashed identity, embedded and attended over, with an LSTM carrying memory across steps, trained with PufferLib 3.0’s PPO. The one thing this setup refuses to do is memorize instances: the problems are random every episode, so the only thing worth learning is the procedure.</p> <h2 id="where-it-came-from">Where it came from</h2> <p>The question came out of algorithms, not machine learning. Working with Dr. Pallavi Jain on ranked aggregation, the open problem in the air was EFX, whether an envy-free-up-to-any-good allocation always exists, known for three agents and open from four onward, and she encouraged me to hunt for a counterexample. Hunting for counterexamples and hunting for algorithms are the same kind of search, discrete, enormous, and allergic to gradients, and I wanted to know whether a learned searcher could walk spaces like that. The first version of the environment says so outright. Before the general tape machine there was poly_time, and its problem was literally a fair-division instance, four agents, a random utility matrix over the items, and the machine’s output was an allocation with a halt flag. PolyTM was what it generalized into, the small, controlled version of the question.</p> <p>It did work, for some of it. The network learned to compose correct computations for the arithmetic tasks, and the work pushed further from there. What it did not give me is the thing I wanted next: you can watch the tape and see <em>that</em> it computes, but not read back <em>what</em> algorithm it found. Explainability was the planned second half, extracting the discovered procedure from the trained policy, and it stayed planned.</p> <hr/> <p><em>Edit: I did not see it at the time, but this was an early run at the idea I now work on. A model that, given a task, produces the computation that solves it rather than grinding out the answer itself, that is the whole shape of my current work on meta-reasoning in language models. PolyTM was the same bet, placed on a tape instead of a trajectory.</em></p>]]></content><author><name></name></author><category term="research"/><category term="rl"/><category term="algorithms"/><summary type="html"><![CDATA[An RL environment where a neural network learns to write the computation that solves a problem, instead of guessing the answer.]]></summary></entry></feed>